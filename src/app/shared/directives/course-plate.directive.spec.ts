import { CoursePlateDirective } from './course-plate.directive';

describe('CoursePlateDirective', () => {
  // it('should create an instance of CoursePlateDirective', () => {
  //   const directive = new CoursePlateDirective();
  //   expect(directive).toBeTruthy();
  // });
});
